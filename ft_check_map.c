#include "so_long.h"

static t_map ft_init_vars()
{
    t_map   map;

    map.c = 0;
    map.e = 0;
    map.p = 0;
    map.x = 0;
    map.y = 0;
    return (map);
}

void    ft_check_map(t_vars *vars)
{
    t_map   map_vars;

    map_vars = ft_init_vars();
    
}